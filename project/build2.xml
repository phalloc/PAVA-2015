<project name="builder" default="run" basedir=".">

	<description>
		simple example build file
	</description>

	<!-- set global properties for this build -->
	<property name="src" location="src" />
	<property name="build" location="build" />
	<property name="args" value="test.Example" />

	<path id="javassist.path">
		<pathelement location="./javassist.jar" />
	</path>

	<target name="init">
		<!-- Create the time stamp -->
		<tstamp />
		<!-- Create the build directory structure used by compile -->
		<mkdir dir="${build}" />
	</target>

	<target name="compile" depends="init" description="compile the source ">
		<!-- Compile the java code from ${src} into ${build} -->
		<javac srcdir="${src}" destdir="${build}">
			<classpath>
				<path refid="javassist.path" />
			</classpath>
		</javac>
	</target>

	<target name="clean" description="clean up">
		<!-- Delete the ${build} and ${dist} directory trees -->
		<delete dir="${build}" />
	</target>

	<target name="run" depends="compile" description="start the project">
		<java classname="ist.meic.pa.DebuggerCLI" classpath="${build}"
			fork="false" >
			<classpath>
				<path refid="javassist.path" />
			</classpath>
			<arg value="test.Example" />
		</java>
	</target>

</project>